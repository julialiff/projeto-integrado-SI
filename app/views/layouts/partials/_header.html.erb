<nav class="navbar navbar-dark bg-dark">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item mr-sm-2">
            <h2><%= link_to "SóMáscaras", :root, class: "site-name" %></h2>
        </li>
    </ul>
    <ul class="navbar-nav ml-auto">
        <li class="nav-item ">
            <% if user_signed_in? %>
            <button class="btn btn-sm btn-outline-primary" type="button"><%= link_to "Sair", :destroy_user_session_path, method: :delete %></button>
            <% else %>
                <button class="btn btn-sm btn-outline-primary" type="button"><%= link_to "Login", :login %></button>
            <% end %>
        </li>
    </ul>
</nav>
